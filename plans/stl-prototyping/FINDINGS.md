# Findings log

**What the build taught, especially where reality contradicted the design.**

**Read this before touching a task**, and read it whole before anything that only a person can
verify. It is the only place a hand-verification is written down: `npm test` is the only
evidence a session can produce on its own, so a ✅ line here is the *entire* record that
something was seen working for real.

**Newest first. About forty words a row.** The long version belongs in the commit message.

**What goes here:** anything the build taught that the next session would otherwise
rediscover; anything noticed but deliberately left alone under the scope rule; every answer
that came back from the user's own hands, with its date.

Legend: 🐞 a defect found · ✅ verified by hand with the user · 📌 worth knowing ·
🔄 a decision the user changed.

| Date | Finding | Consequence |
|---|---|---|
| 2026-08-28 | 📌 **T04: a quantised weld key reports a closed mesh as open, and it is not a rare case.** The float32 immediately below 1.0 sits **5.96e-8 mm** from it — sixteen hundred times inside the 1e-4 tolerance — yet quantises to cell 9999 while 1.0 quantises to 10000. Any seam landing on a grid line does this, half the time. | `tasks/T04` specifies the quantised key; `core/solidity.js` welds by **distance**, bucketing into cells of one tolerance and searching the 27 around each vertex. Same cost, and no false "not watertight" — which the task doc itself calls the worse failure. Deviation recorded in PROGRESS.md. |
| 2026-08-28 | 📌 **T04: a coordinate can be a real number and still be unweldable.** Above `weldTolerance * 2^53` — about **900 000 km** at the default — `coordinate / tolerance` stops yielding distinct integers, so "the same point" has no meaning. `outOfRangeCoordinateCount` counts those triangles and sets them aside, separately from `BAD_COORDINATE`. | Covers T03's open hole for free: float32 area overflow starts at ~1.3e19 mm, far past this line, so any mesh that hits it is already flagged. **Still the user's call** whether T07 gives it its own code (recommended), folds it into `BAD_COORDINATE`, or drops the threshold to ten metres. |
| 2026-08-28 | 📌 **T04: `tasks/T04`'s expectation for `degenerate.stl` is wrong.** It asserts the mesh "is still reported watertight". The fixture is one honest triangle plus one collinear one — a lone triangle is an open surface and can never be watertight. Its real intent is that the *degenerate* triangle adds no boundary edges. | Tested as the doc meant it rather than as it said: `degenerate.stl` gives 3 boundary edges and `degenerateTriangleCount` 1, and a separate cube-plus-sliver case proves a closed solid stays closed. Any later task quoting that line should read this row first. |
| 2026-08-28 | 📌 **T04: `requirePositions` now exists twice, in `core/mesh.js` and `core/solidity.js`.** Identical contract, identical error types. `core/mesh.js` does not export it and T04 may not edit T03's file. | Left alone under the scope rule. Every remaining core module that takes `positions` — `overhangs`, `fit` — will want it too, so a third copy is the point to stop and export one. Cheap either way; nobody's behaviour depends on it. |
| 2026-08-28 | 📌 **T03 review: an exact 45° chamfer lands on the wrong side of a band edge about 4% of the time.** Of 1060 geometrically-exact 45° downward faces, **39 measured above 45°** once the coordinates passed through float32 — scatter −9.8e-7 to +3.8e-6 degrees. Under `DESIGN.md §2.5` those are `steep` ("will show sag"), not `mild` ("fine on this printer"). | **T05 must decide what happens within float noise of a band edge**, and it is user-visible: a 45° chamfer is the commonest feature on a printed part, and the report would contradict itself between two identical parts. The normals are as good as float32 allows — this is not fixable in `core/mesh.js`. A tolerance at the five band edges is the cheap answer; the rule is the user's. |
| 2026-08-28 | 🐞 **T03 review: `triangleAreas` returns `Infinity` for coordinates float32 holds perfectly legally.** Above roughly 2.6e19 mm a triangle's area exceeds float32, so the Float32Array stores `Infinity` while `surfaceArea` (float64) stays finite — the parts stop summing to the whole. Not the `BAD_COORDINATE` case: 1e30 *is* a real number, and it parses, measures and writes back without complaint. | Reachable from a runaway scale in a recipe, or a corrupt file whose length happens to satisfy 84 + 50n — a quarter of random float32 bytes clear the threshold. **Nothing detects it today.** Left alone under the scope rule: what the report says about such a mesh is the same product decision the user already made for `BAD_COORDINATE`. `core/mesh.js` now states the hole rather than claiming the invariant. |
| 2026-08-28 | 📌 **T03 review: verified sound where it matters.** Volume, area and centroid cross-checked against an independent decomposition about a different anchor on a 320-triangle icosphere: agreement to **2.7e-15**, every normal outward and unit. Twenty fresh mutations planted by the reviewer, **all twenty caught**. The centroid's bounding-box guard never misfired — a 0.01 mm plate at 400 000 mm still measures its true centre. | The ✅ on T03 is worth what it says: the arithmetic is right, and the suite defends it rather than merely covering it. A plain array holding a non-number (a numeric string, `undefined`) still leaks straight into `boundingBox`'s min/max — caller-bug path only, unreachable from `parseStl`, left alone. |
| 2026-08-28 | 🔄 **Decision, and it closes the third row below: a mesh with a broken coordinate is reported, not refused.** `BAD_COORDINATE` error naming the triangle; the mesh still loads and still previews. Measured first: one `Infinity` vertex gives bbox `Infinity mm`, volume `NaN`, normals `NaN`, centroid `NaN`. | `DESIGN.md §2.10` now carries the row. **T04 owns the detection and T07 the wording**; T03 deliberately did not implement it, under the scope rule. T03's `triangleNormals` guard catches a `NaN` cross product but not an `Infinity` one — T04 must not assume the normals are clean. |
| 2026-08-28 | 📌 **A measured volume is good to about three significant figures, and the limit is the file, not the arithmetic.** 80 000 triangles centred 400 000 mm from the origin measure their own volume 6.7e-4 off; accumulating in float32 instead lands no further out. An STL stores float32 and that error cannot be recovered. | **T07 must not print a volume or a filament figure to eight digits** — it would be inventing precision the mesh does not carry. Accumulating in float64 is still right (it is free and adds nothing), but the suite cannot demonstrate it, and `core/mesh.js` says so rather than claiming a wall where there is a door. |
| 2026-08-28 | 📌 **A cube is a useless fixture and the tetrahedron earns its keep.** Mutation-testing the T03 suite: with only the cube, `volume/3` instead of `/6`, a flipped cross-product term and an unhalved area all still passed — the cube's exact small integers and axis-aligned faces hide them. All fifteen deliberate defects are now caught. | Any later measurement task wants a fixture with no axis-aligned face and no round-number answer. `test/fixtures/tetrahedron.stl` (edge 10, volume 117.851, area 173.205) is there for exactly this and is checked in the suite against its own closed form, not against a stored run. |
| 2026-08-27 | 🐞 **T02 review: `NOT_STL` was unreachable for any file over 84 bytes.** Everything longer reached the binary parser, so an `.scad` recipe or an error page earned *"the header declares 744845417 triangles"*. Text not beginning with `solid` is now `NOT_STL`. | Binary rubbish stays `TRUNCATED` — bytes that are not text cannot be told from a damaged mesh. T07 and T09 can rely on `NOT_STL` meaning "wrong file", which is the commonest way a user gets here. |
| 2026-08-27 | 🐞 **T02 review: the writer's Infinity guard checked float64 and wrote float32.** `Number.isFinite(1e40)` is true and `setFloat32` stores it as `Infinity`, so the check let through exactly the file it exists to prevent. Now `Math.fround(value)`. | Any later check on coordinates must test the **stored** precision, not the value handed in. Reachable only from a plain array or `Float64Array`, which is why the original tests missed it. |
| 2026-08-27 | 📌 **Unsettled, and the user's call: a file with an infinite or NaN coordinate parses fine and cannot be written back.** ASCII `1e40` and those bit patterns in a binary both pass through into `positions`; `writeBinaryStl` then refuses the same mesh. | T03's bounding box would report `Infinity` mm and T07 would print it. Decide before T03 whether the parser rejects such a file, or the report names the bad triangle. Left as-is meanwhile. |
| 2026-08-27 | 📌 **`node --test` executes every `.js`/`.mjs`/`.cjs` under `test/`.** `tasks/T02` put the fixture generator at `test/fixtures/make-fixtures.mjs`, where it would have rewritten the fixtures on every `npm test` — and silently repaired a corrupted one. Moved to `tools/make-fixtures.mjs`. | **Nothing executable may live under `test/` unless it is a test.** Any later task adding a helper, a generator or a shared fixture module there hits the same wall. |
| 2026-08-27 | 📌 **`detectFormat` has a third case its two-value return does not show.** A file that is neither valid-binary-by-arithmetic nor plausible text is reported `binary`, so a truncated file fails as `TRUNCATED` naming both byte counts, instead of an ASCII syntax complaint about a file holding no text. | The tie-break is "is this text?", not "is this ASCII STL?". T07 shows these codes to the user, so which error a corrupt file earns is a product-visible choice, not an internal one. |
| 2026-08-27 | 🐞 **T01 review: the boundary check passed on code it never read.** It globbed `*.js` and filtered on `isFile()`, so a `.mjs` or `.cjs` module in `core/` — Node executes both identically — and a symlinked one were never opened. A backtick specifier also slipped the pattern. All four fixed and proved with planted files. | Any later check that walks `core/` must use the same extension set and `!isDirectory()`. **A test that reports a wall where there is a door is worse than no test**, because nobody looks again. |
| 2026-08-27 | 📌 **Two boundary rules are stated in `DESIGN.md` but enforced by nothing.** A `core/` file importing `'../shell/cad.js'` passes (§3.2's dependency direction), and so does importing a bare package like `'lodash'` (§5's zero-dependency rule). Both verified. | Left alone under the scope rule: these are *new* rules, not holes in a specified one, so they are the user's call. Cheap to add to `test/boundary.test.js` if wanted — resolve relative specifiers against the file's own path and flag anything landing outside `core/`. |
| 2026-08-27 | 📌 **T00 review re-ran every `DESIGN.md` §5.3 command and all of it holds**: symlink path, version 2026.06.12, `--export-format binstl` (684 B = 84 + 50×12, true binary), ASCII default at 2.2×, headless PNG in 0.1 s, `step` rejected verbatim. **OpenSCAD's binary STL header starts `OpenSCAD Model`, not `solid`.** | The documented commands are safe for T08 to build on unread. T02 should not expect its own writer's header back from an OpenSCAD file, and gets a real ASCII-vs-binary fixture pair for free from `-o x.stl` vs `--export-format binstl`. |
| 2026-08-27 | 🐞 **T00 review: one required comparison row was never filled** — "dimensions readable as named constants", the legibility row. `DESIGN.md §7` asserts legibility qualitatively but nothing measured it, and `spike/` is deleted and was never tracked, so it can no longer be filled. | Accepted rather than reopened: the row would not have changed a decision the user has already made on speed, one runtime and install size. Recorded so the ✅ on T00 is not read as "every row measured". |
| 2026-08-27 | 🔄 **Decision: OpenSCAD is the recipe language.** User's call, on T00's measurements, after a simple bracket, a hard duct adapter and a photo-sourced model were built both ways. `DESIGN.md §5.3` now names the binary, the install command and the mandatory `--export-format binstl`. | T00 is closed and Phase 2 is unblocked. **STEP export is given up permanently.** The dissenting evidence — build123d refuses impossible geometry, OpenSCAD approximates silently — is recorded in `DESIGN.md §7` rather than dropped, because it is the argument that would reopen this if fillets ever start mattering. |
| 2026-08-27 | 🐞 **One oblique photo was not enough.** From a single angled shot the box positions and the hang-tab cutout were both wrong — the top carton was guessed rotated 14° and overhanging when it is near-parallel and fully supported, and a keyhole was drawn where the real cutout is a rounded cross. **Two orthographic views (top + side) fixed both in one pass.** | Photo-to-model needs a **top and a side view**, not a nice-looking three-quarter shot. Perspective destroys exactly the relative placement and cutout shape that matter. Worth stating in `MODELLING.md` (T14) as what to ask the user for. |
| 2026-08-27 | 📌 A folded flap can be measured without measuring it: the tab's foreshortened length in the top view (28.4 mm) and its tip touching the table in the side view (drop 40 mm) together give 54.6° and a true length of 49.1 mm. | Two orthographic views recover geometry neither shows directly. Cheap, and it beats asking the user to unfold and re-photograph. |
| 2026-08-27 | 📌 **Photo → model works.** From one photograph of two stacked cartons, OpenSCAD produced a recognisable model in 0.11 s: proportions, the hang tab with its keyhole, the perforated dot row, engraved lettering. Print graphics become shallow relief, since **an STL carries no colour**. | Establishes a real workflow: the user photographs a thing and asks for it. `MODELLING.md` (T14) should state the colour limitation plainly, and that dimensions from a photo are guesses needing a caliper before anything load-bearing. |
| 2026-08-27 | 🔄 **User rule, confirmed:** requirements that contradict each other are the user's to resolve, not the session's, and a session must be **verbose** about them — arithmetic, every option with its cost, a recommendation — and **wait**, rather than building under a stated assumption. | Tightens CLAUDE.md's existing rule with an explicit style requirement. T00 round 2 got this wrong: it built the duct under three flagged assumptions instead of stopping. Correct behaviour was to raise all three in full and hold. |
| 2026-08-27 | 🔄 **User rule, in response to the mirrored marking:** build the design, render it, look at the picture, and iterate until it is right **before** showing them. Passing measurements are not sufficient evidence that a part is correct. | Binds every session that produces geometry, and is a required rule in `MODELLING.md` (T14). It covers *appearance* defects only — a contradictory or underspecified requirement still goes to the user before building, per CLAUDE.md. Cheapest with OpenSCAD, which renders headlessly in ~0.3 s. |
| 2026-08-27 | 🐞 The raised marking came out **mirrored in both implementations** — the same spatial-orientation slip made twice, once per language. Exact bbox: passed. Watertight: passed. Volume: identical. **Only rendering it and looking caught it.** | The strongest argument yet for T11's viewer being load-bearing rather than a convenience. Any text or asymmetric feature must be *seen*, and `MODELLING.md` (T14) should say so. Rendering a PNG via `openscad -o x.png --viewall --autocenter` is a cheap self-check available today. |
| 2026-08-27 | 🐞 **The hard part exposed the real difference, and it is not speed.** The brief's R3 junction fillet is *geometrically impossible* — OCC's `max_fillet` puts the true limit at 2.67 mm. build123d **refused and said so**. OpenSCAD silently produced a clipped approximation and reported success. | The bbox was exact and the mesh watertight in **both** cases, so **none of the project's planned automated checks would have caught it.** A tool that refuses the impossible has value this design cannot otherwise buy. Weigh it against OpenSCAD's speed rather than assuming the earlier recommendation still holds. |
| 2026-08-27 | 📌 Same duct, both toolkits: the *lofts* agree to **0.46 %**, but the finished parts differ by **2.50 %**. The gap is entirely the fillet — OpenSCAD's added +292 mm³, build123d's **removed −183 mm³**, because filleting the whole junction loop necessarily rounds the flange's convex outer edge too (mid-edge, they are the same edge in space). | "Add an R3 fillet" is not a well-defined instruction on this geometry, and the two tools resolve it differently and visibly. Any recipe with a fillet needs its intent stated in prose, not just a radius — a note for T14's modelling rules. |
| 2026-08-27 | 📌 Duct adapter, measured: OpenSCAD **0.28–0.39 s**, 31 280 tris, 66 code lines, **1 attempt**; build123d **2.4 s**, 50 238 tris, 54 code lines, **3 attempts plus 2 diagnostic scripts** (all fillet-related). Both exact 80 × 80.6 × 54, both watertight. | The 1-vs-3 attempt count **flatters OpenSCAD**: its offset-based blend cannot fail, so it never established that a fillet was possible. Attempts-to-green measures loudness of failure, not correctness — do not quote it as a reliability score. |
| 2026-08-27 | ✅ **Verified by hand with the user.** OpenSCAD 2026.06.12 installed via `brew install --cask openscad@snapshot`. Every CLI invocation hung indefinitely — killed at 60 s — until the user accepted a Gatekeeper "downloaded from the internet" modal waiting on their screen. | `spctl` reported the app *accepted and notarized* while it was still blocked, so Gatekeeper state is not a usable readiness check. **A first-run CAD hang is a dialog until proven otherwise.** T08's timeout must say so in its error text, or the first user to hit it will think the build pipeline is broken. |
| 2026-08-27 | 📌 OpenSCAD exports **ASCII** STL by default; `.step` is rejected outright and `step` is not a known format. Supported: stl, off, wrl, amf, 3mf, csg, dxf, svg, pdf, png. | T08 must pass `--export-format binstl` explicitly. **No STEP export**, confirming the prior's expected loss — DESIGN.md §8 already puts STEP out of scope. 3MF *is* available if that is ever revisited. |
| 2026-08-27 | 📌 Head-to-head on the same L-bracket: OpenSCAD **0.06–0.08 s**, 974 triangles, 149 MB, no second runtime; build123d **45.9 s cold / 1.7 s warm**, 1858 triangles, 462 MB, Python 3.12. Both exact 40×30×25, both watertight, volumes agree to 0.002%. | The prior's expected loss on fillets was **disproved**: OpenSCAD's native `offset(r=2)` rounded the base corners in one line, no BOSL2. Caveat — only *vertical-edge* rounding was tested; true 3D edge fillets remain OpenSCAD's weak spot. |
| 2026-08-27 | 📌 build123d 0.11.1 on Python 3.12 built the T00 L-bracket **correct on the first attempt**: exact 40×30×25 bbox, watertight, 1858 triangles, STEP export working. Install is 462 MB (46 packages incl. numpy/scipy/scikit-learn). | Candidate B is a real contender, not a straw man. But **46 s cold vs 1.7 s warm** — the cold run alone blows through DESIGN.md §5.2's 30 s build timeout, so choosing it would force that seatbelt to be re-thought. |
| 2026-08-26 | 📌 Node 24's built-in test runner works here with zero dependencies installed — `node --test` on a bare directory ran green. | `npm test` needs no test framework. The project's runtime dependency count stays at zero. |
| 2026-08-26 | 📌 Homebrew's `openscad` cask is **2021.01 and deprecated — disabled 2026-09-01**, six days from planning. The current build is a separate cask, `openscad@snapshot`, at 2026.06.12. | If T00 chooses OpenSCAD it must install `openscad@snapshot`. A session reaching for the obvious `brew install --cask openscad` will hit a wall. The snapshot is also the build with the Manifold backend, which matters for watertight output and speed. |
| 2026-08-26 | 📌 Confirmed from Creality's own listing rather than memory: Ender-3 V3 KE is **220 × 220 × 240 mm, 0.4 mm nozzle, 300 °C max hotend, direct drive**. | Build-volume checks use 220/220/240 less a 5 mm per-axis margin. 300 °C means PETG is comfortable. Direct drive means bridging and PETG behave better than on older Ender-3s, which is why the overhang bands are set at 45°/60°/85° rather than more conservatively. |
| 2026-08-26 | 📌 Nothing 3D-related is installed on this machine: no OpenSCAD, Blender, FreeCAD, MeshLab, admesh, f3d, and **no slicer of any kind**. System Python is 3.9.6. | Every workflow must assume a bare machine. Anything Python-shaped goes through `uv`, never `/usr/bin/python3`. No design may lean on a slicer — which is also why proper thin-wall detection is out of scope. |
| 2026-08-26 | 📌 Binary STL files frequently begin with the ASCII text `solid`, because writers put a name in the 80-byte header. | ASCII-vs-binary detection must test whether `80 + 4 + 50 × triangleCount` equals the file length, never the `solid` prefix. T02 tests this explicitly. |
